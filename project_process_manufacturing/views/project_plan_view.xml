<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="action_project_plan_orders" model="ir.actions.act_window">
        <field name="name">Plan Orders</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">tree,kanban,form,calendar,pivot,graph,activity</field>
        <field name="view_id" ref="sale.view_quotation_tree_with_onboarding"/>
        <field name="context">{}</field>
        <field name="domain">[('state', '=', 'to approve')]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new quotation, the first step of a new sale!
            </p><p>
                Once the quotation is confirmed, it becomes a sales order.<br/> You will be able to create an invoice and collect the payment.
            </p>
        </field>
    </record>

    <!-- Main menu for Plan Project in Project -->
    <menuitem id="menu_sale_project_plan"
            name="Plan Project"
            action="action_project_plan_orders"
            parent="project.menu_main_pm"
            sequence="0" groups="group_plan_project"/>

</odoo>
