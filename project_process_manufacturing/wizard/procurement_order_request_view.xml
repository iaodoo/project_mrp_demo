<?xml version="1.0"?>
<odoo>

        <record id="view_procurement_order_request_form" model="ir.ui.view">
            <field name="name">procurement.order.request.form</field>
            <field name="model">procurement.order.request</field>
            <field name="arch" type="xml">
                <form string="Procurement Request">
                    <group>
                        <field name="project_id" invisible="1"/>
                    </group>
                    <group string="Raw Material">
                        <field name="procurement_raw_material_ids" nolabel="1">
                            <tree editable="bottom">
                                <field name="product_id" required="1"/>
                                <field name="partner_id" domain="[('supplier', '=', True)]"
                                context="{'show_supplier':True,'product_id':product_id}"
                                required="1"
                                string="Vendor"
                                />
                                <field name="order_qty" required="1"/>
                                <field name="qty_available" force_save="1"/>
                                <field name="cost" force_save="1"/>
                            </tree>
                        </field>
                    </group>
                    <footer>
                        <button name="generate_purchase_order" string="Create Procurement" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_procurement_order_request" model="ir.actions.act_window">
            <field name="name">Procurement Order</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">procurement.order.request</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_procurement_order_request_form"/>
            <field name="target">new</field>
        </record>
</odoo>
